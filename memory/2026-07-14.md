# memory/2026-07-14.md

## Features Shipped
- **Repo merge: quotecore-website → quotecore-plus** (commits `7778e84` → `7809738`, merged to both `development` and `main`)
  - All marketing pages copied into `app/(marketing)/` route group: about, services, blog (10 posts), contact, free-trial, coffee-terms, cookie-policy, construction-quoting-software, roofing-quoting-software, home
  - Marketing components at top-level `components/`: BlogHeader, SiteFooter, CookieConsent, AttributionTracker, SiteAssistant, CoffeePopup, EarlyAccessPopup, etc.
  - Marketing lib at top-level `lib/`: analytics, consent, faqs, pricing, schema, supabase
  - Marketing API routes: assistant-contact, contact, leads, currency
  - FreeToolsAuthCard + FreeToolsHeader copied to `app/(public)/_components/`
  - Domain-aware homepage: `app/page.tsx` checks host header → marketing for quote-core.com, app landing for app.quote-core.com
  - Middleware updated: marketing paths added to PUBLIC_PATHS, NZ domain detection added
  - MDX support added (`@next/mdx`, `@mdx-js/loader`, `@mdx-js/react`, `@tailwindcss/typography`, `@vercel/analytics`)
  - Marketing CSS merged into globals.css
  - 51 marketing public assets (images, PDFs, videos) copied
- **Domain migration**: `quote-core.com` + `www.quote-core.com` moved from `quotecore-website` Vercel project to `quotecore-plus-main` Vercel project
  - DNS TXT records updated in Cloudflare by Shaun
  - `quote-core.com` → 308 redirect → `www.quote-core.com`
  - Both domains verified and live
- **Build fixes**:
  - OpenAI client lazy-init (placeholder fallback at build time) in `app/api/free-tools/parse-document/route.ts` and `app/api/app/parse-document/route.ts`
  - Free tools Supabase client build-safe fallback in `app/lib/supabase/free-client.ts`
  - Free tools Supabase env vars added to `quotecore-plus` Vercel project (preview builds)

## Decisions
- Single repo `quotecore-plus` serves both domains (marketing + app)
- React 18 retained (not upgraded to 19)
- Marketing pages in `(marketing)/` route group with own layout
- `quotecore-website` repo still exists but is no longer deployed (domains moved)

## Bugs / Risks
- **Shaun found problems on initial testing** — he said issues I claimed were fixed are still present (6th time this has happened). Need to investigate specific issues in next session.
- `colorScheme` metadata warnings on several pages (non-fatal but should move to viewport export)
- The `quotecore-plus` Vercel project (old/legacy, connected to GitHub) is separate from `quotecore-plus-main` — env var management is split across projects
- FreeToolsAuthCard has been copied but NOT yet integrated into the app's existing free tools pages (they still use the old FreeToolsAuthProvider/Button flow)

## Fixes Tried
- OpenAI client init: added `'placeholder'` fallback for missing `OPENAI_API_KEY` at build time
- Supabase free client: added placeholder URL/key fallback when env vars not injected at build/prerender time

## Follow-Ups
- **Investigate Shaun's reported issues** — he found problems on testing despite me saying everything was fixed
- Fix `colorScheme` metadata warnings (move to viewport export)
- Integrate FreeToolsAuthCard into app's free tools pages
- Archive `quotecore-website` repo after confirming everything works
- Clean up old `quotecore-plus` Vercel project (legacy, not the dev/main projects)
- Consider consolidating env vars across Vercel projects

## Promote To MEMORY.md?
- Repo merge: YES — durable architectural change
- Domain migration: YES — current deployment topology changed
- Build fixes (OpenAI/Supabase lazy init): YES — pattern for future build-time env var issues
- Shaun's reported issues: NO — not yet diagnosed, wait for next session
