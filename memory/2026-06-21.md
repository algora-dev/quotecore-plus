# Daily Note — 2026-06-21

## Session summary
Full go-live day: dev→main merge, full Gerald security audit cycle, all blockers resolved, remediation on main.

---

## dev → main merge (go-live bundle)

- Merged 51 commits from `development` into `main` → new `main` HEAD `70b0ca4`.
- Audit range `3d8d311..70b0ca4`: 79 files, +6,836/−1,903.
- Gerald audit brief: `docs/audits/GERALD-BRIEF-2026-06-21-GOLIVE-BUNDLE.md`.

**Bundle contents:**
- Quantity column + shared `AddLineItemModal`
- Per-line margin system + Original/Current snapshot tabs
- Quote expiry (auto-expire cron + alerts)
- Quote notes (DB-backed CRUD + collapsible panel)
- Invoice-from-quote + order-from-quote full-page line selectors
- Free-tier invoice gate + consistent nav gating
- Mid-quote Smart Component™ creation (`CreateSmartComponentModal`)
- Takeoff left sidebar redesign (card layout, three-dot menu, search)
- Smart Components™ rebrand + Tutorials button + "Start Here" card
- Item Cost rename throughout
- Send/token H-06/H-07 fixes (stale token, committed token body)
- 7 DB migrations (qty col, hide-price toggles, margin system, quote_notes ×3, base_unit_cost patch)

---

## Gerald go-live security audit — full cycle

### Report 04 (initial) — 0 Crit, 4 High, 6 Med, 4 Low

**Remediation R1 (commit `35ca401`):**
- H-01: `replace_customer_quote_lines()` atomic RPC (advisory xact lock, ownership check, delete+insert in one tx)
- H-02: `base_unit_cost` added to canonical timestamped migration `20260621120000`
- H-03: invoice-from-quote throws on tampered/empty selection (`selectionProvided` flag, no fallback)
- H-04: `respondToQuote` lifecycle-guarded final update + 0-row conflict check before alerts
- M-01: CHECK constraints on qty/price/margin fields
- M-02: `trg_cql_component_same_quote` trigger enforces component belongs to same quote
- M-03: `failClosed: true` on invoice dispute/payment, message reply, revision, order respond, all recovery limiters
- Migration `20260621120000` applied; RPC, trigger, 5 constraints, column all verified.

### Report 05 (re-audit R1) — 2 remaining High blockers

**Remediation R2 (commit `4b2dbb2`):**
- H-01-R1: RPC now checks `auth.uid()` membership in `public.users` before delete/insert; service_role null path acceptable (already gated by `requireCompanyContext()`).
- H-03-R1: `new-from-quote/page.tsx` detects `lines` key presence via `'lines' in resolvedSearchParams`; `invoices/actions.ts` treats `selectedLineIds !== undefined` as selection-provided; empty array throws immediately. Closes all 3 tamper vectors (`?lines=bogus`, `?lines=`, `?lines=,,,`).
- Migration `20260621130000` applied.

### Report 06 (re-audit R2) — CLEARED ✅
- H-01-R1 and H-03-R1 both confirmed cleared.
- Deferred: M-04 (quote_notes immutable trigger), M-05 (dep advisories), M-06 (HSTS+CSP), L-01–L-04.

---

## Final dev → main merge (remediation)

- Merged remediation commits (`35ca401`, `1e5698a`, `4b2dbb2`, `c6ebc35`) to `main` → HEAD `61cb7b1`.
- Both branches now even.

---

## Deferred / next actions
- M-04 quote_notes immutable-column trigger
- M-05 `ws` + `dompurify` dep updates (separate patch branch)
- M-06 global HSTS + app CSP
- L-01–L-04 (auth API rate limits, token+IP buckets, env startup validation, secret scanner)
- Live Stripe prices still need migrating before real payments (`stripe_price_id_live` trap — see MEMORY.md PRICING section)
