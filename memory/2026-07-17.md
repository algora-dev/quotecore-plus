# memory/2026-07-17.md

## Features Shipped
- AI Takeoff Phase A shipped to `feature/ai-takeoff` (commits `4406af4`, `04821de`)
  - Migration `20260717120000_ai_takeoff_system_components.sql` applied to live DB
  - `component_library.is_system` column + partial unique index
  - `company_component_count` edited (copy-exact-body + `AND is_system = false`)
  - `ensure_ai_system_components(p_company_id)` SECURITY DEFINER RPC
  - `takeoff_pages.ai_scan_result` jsonb column
  - `ai_scan_usage` table with RLS
  - `database.types.ts` regenerated
  - `AI_TAKEOFF_ENABLED=true` in `.env.local`
  - 6 system components seeded for T3 Play (manual SQL — RPC needs auth context)
  - Build passes, pushed to GitHub, Vercel preview deploying

## Decisions
- Phase A executed on GLM 5.2 as specified
- Used direct SQL insert for T3 Play seeding (Management API runs as superuser, `current_company_id()` returns NULL without auth context). The RPC will work correctly when called from the takeoff page server load with a real user session.

## Bugs / Risks
- `ensure_ai_system_components` RPC cannot be tested via Management API alone (needs `auth.uid()` context). Will be exercised in Phase B when `takeoff/page.tsx` calls it during server load.

## Follow-Ups
- Phase B: visibility + seeding call sites (`takeoff/page.tsx` awaits RPC, `is_system` in select, filter from library + manual selector)
- Phase C: AI scan API + prompt (go/no-go checkpoint with Shaun)
- Need to set `AI_TAKEOFF_ENABLED=true` on Vercel preview environment for Phase B+ testing

## Promote To MEMORY.md?
- Not yet — Phase A is foundation only. Will promote when feature is merge-ready.
