# memory/2026-07-17.md

## Features Shipped
- AI Takeoff Phase A shipped to `feature/ai-takeoff` (commits `4406af4`, `04821de`)
  - Migration `20260717120000_ai_takeoff_system_components.sql` applied to live DB
  - `component_library.is_system` column + partial unique index
  - `company_component_count` edited (copy-exact-body + `AND is_system = false`)
  - `ensure_ai_system_components(p_company_id)` SECURITY DEFINER RPC
  - `takeoff_pages.ai_scan_result` jsonb column
  - `ai_scan_usage` table with RLS
  - `database.types.ts` regenerated
  - `AI_TAKEOFF_ENABLED=true` in `.env.local`
  - 6 system components seeded for T3 Play (manual SQL — RPC needs auth context)
  - Build passes, pushed to GitHub, Vercel preview deploying
- AI Takeoff Phase B shipped (`5430216`)
  - `takeoff/page.tsx`: fetch company `default_trade`, check `AI_TAKEOFF_ENABLED`, await `ensure_ai_system_components` RPC before component fetch, add `is_system` to component select
  - `TakeoffPage.tsx` + `TakeoffWorkstation.tsx`: `is_system` on Component interface, `aiTakeoffAvailable` prop, filter from manual add-component selector
  - `components/actions.ts`: `loadComponentLibrary` filters `is_system = false`
- AI Takeoff Phase C shipped (`7f3e215`, `1e9fb05`)
  - `app/lib/takeoff/ai-prompt.ts`: vision prompt (Shaun's refined version) + json_schema for OpenAI structured output
  - `app/api/takeoff/ai-scan/route.ts`: session auth, quote ownership, kill switch, roofing trade gate, per-page consumption guard, sharp EXIF+downscale, gpt-4o structured output, server-side validation, ai_scan_usage logging, maxDuration=60
  - `scripts/test-ai-scan.mjs`: CLI test harness
- AI Takeoff Phase D shipped (`32bda45`)
  - `app/lib/takeoff/applyAiResults.ts`: pure functions — coordinate mapping, snapAndValidate, point-in-polygon, value computation, scale cross-check, resetFromStoredScan
- AI Takeoff Phase E shipped (`93fa883`, `d87b319`)
  - `TakeoffWorkstation.tsx`: AI scan state, handleAiScan (captures canvas PNG, POSTs to API), handleApplyAiResults (creates Fabric objects, updates React state), 'Use AI Assist' button in post-calibration popup, scanning overlay, error toast
  - `AiResultsModal.tsx`: results review modal with summary, per-type breakdown, pitch override, acknowledgment checkbox, apply/discard
  - `aiOrigin` field on ComponentMeasurement interface

## Decisions
- Phase A executed on GLM 5.2 as specified
- Used direct SQL insert for T3 Play seeding (Management API runs as superuser, `current_company_id()` returns NULL without auth context). The RPC will work correctly when called from the takeoff page server load with a real user session.

## Bugs / Risks
- `ensure_ai_system_components` RPC cannot be tested via Management API alone (needs `auth.uid()` context). Will be exercised in Phase B when `takeoff/page.tsx` calls it during server load.

## Follow-Ups
- Phase B: visibility + seeding call sites (`takeoff/page.tsx` awaits RPC, `is_system` in select, filter from library + manual selector)
- Phase C: AI scan API + prompt (go/no-go checkpoint with Shaun)
- Need to set `AI_TAKEOFF_ENABLED=true` on Vercel preview environment for Phase B+ testing

## Promote To MEMORY.md?
- Not yet — Phase A is foundation only. Will promote when feature is merge-ready.
