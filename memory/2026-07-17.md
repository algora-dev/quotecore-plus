# memory/2026-07-17.md

## Features Shipped
- **AI Takeoff Phases A–E complete** on `feature/ai-takeoff` branch (commit `5d69f0d`).
  - Phase A: DB migration `20260717120000` — `is_system` column, `ai_scan_result` on takeoff_pages, `ai_scan_usage` table, `ensure_ai_system_components` RPC. Applied to live DB.
  - Phase B: Visibility + seeding — `takeoff/page.tsx` calls `ensure_ai_system_components`, `is_system` filtered from manual component selector.
  - Phase C: AI scan API route + Shaun's refined prompt. `sharp` EXIF+downscale, GPT-4o structured output, server-side validation.
  - Phase D: `applyAiResults.ts` — pure functions for coordinate mapping, snapping, validation, value computation.
  - Phase E: Workstation integration — `handleAiScan`, `handleApplyAiResults`, `AiResultsModal`, scanning overlay, error toast.

## Decisions
- Canvas capture switched from PNG to JPEG (q=0.8) to stay under Vercel's 4.5MB request body limit.
- AI Assist button placed above Skip in post-calibration popup.
- Error messages now surface actual API errors instead of generic "Network error".
- `OPENAI_API_KEY` and `AI_TAKEOFF_ENABLED=true` set on Vercel preview environment for `quotecore-plus` project.

## Bugs / Risks
- **AI scan still failing on Vercel preview** — the last deploy (`5d69f0d`) surfaces the real error now. Shaun tested and got a full error message but session was too large to continue. **Next session: get the exact error text from Shaun and debug.**
- Likely causes to investigate: OpenAI API key invalid/expired, `sharp` native module issues on Vercel serverless, function timeout (60s may exceed plan), `requireCompanyContext` failing in serverless.
- Vercel token (`VERCEL_TOKEN`) keeps expiring mid-session — annoying for deployment URL lookups.

## Follow-Ups
- **NEXT SESSION:** Get the exact AI scan error from Shaun and fix it.
- Phase F (Review UX) and Phase G (Production hardening) not started.
- Smoke test checklist has pending items for AI takeoff.

## Promote To MEMORY.md?
- AI Takeoff env vars on Vercel (`OPENAI_API_KEY`, `AI_TAKEOFF_ENABLED`) — already in MEMORY.md if it was promoted. Check.
- Vercel preview deployment URL pattern: `quotecore-plus-[hash]-algora-devs-projects.vercel.app` (NOT commit-hash based).
