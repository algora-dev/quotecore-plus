# 2026-06-12 (Gavin daily note)

> Single session, long. dev HEAD `67edb0e`, **246 commits ahead of `main`**. Build green (last `next build` passed at `67edb0e`). All work on `development`, untested by Shaun unless noted. Earlier in the day: Pricing Tier v2 billing-panel UI + smoke docs (commits up to `8a06264`) — see git log. This note covers the **Tutorials + post-tutorial review batch** (the bulk of the session).

## Shipped this session

### Tutorials page (NEW) — `/[ws]/tutorials`
- `70de0d9` v1: 13-card Resource-Library-style hub. Each card opens `TutorialModal` (paged what/how/when/why, Back/Next + dots). Two CTAs: **Go to feature** (accent) + **Walk me through with Q** (black).
- **Q-launch bridge (Option A):** `app/components/assistant/startGuide.ts` dispatches `qcp:start-guide` CustomEvent; `AssistantWidget` listens, opens panel, calls `engine.startWorkflow(workflowId, pathname)`. No LLM round-trip.
- Card→guide map uses REAL ids from `guides.generic.ts` (Drawings/Invoices/Message Center have guides; Templates/Follow-ups/Downloading/Q&Docs hide the Q button).
- Entry points: Help/Docs slider "Tutorials" button; Resources page button; Account→Support "Help & learning" card (Tutorials + Open help docs); first-login **WelcomeModal** on dashboard (gated `users.tutorials_seen_at`, mig `20260612180000`).
- `c9d3519` review round 1: copy rewrites for all 13 cards; new pages (Catalogs Why/Maps, Follow-ups How-to-create, Templates Payment, Message Center Why); renames (Catalogs templates heading, Orders "Custom Order"). **Global: replaced ALL em-dashes `—`→`-` app-wide (550, 126 files).** Fixed latent literal `\uXXXX` escapes in tutorials.data that would have rendered raw. Modals: close on X only (no overlay click). Orange hover glow on Resources/Account Tutorials buttons. First-visit `/tutorials` intro modal (localStorage).
- `1a55901` review round 2: intro modal copy; Attachments "What" copy; landing tagline (`app/page.tsx`) → "Measure, quote, order, track, invoice, store, all in 1 place"; **billing DB copy** (trial display_name → "14 day full feature trial"; pro blurbs drop "Everything in Growth"→"Everything in Starter" + "Image drawing tool"; pro_plus "image-drawing capacity"; `features.ts` label "Flashing drawings & library"→"Image drawings & library"); Invoice editor **Send Invoice moved far-right** (last action).
- **NAV-BLOCKING FIX (`1a55901`):** tutorial "Walk me through with Q" no longer `router.push()`s before starting the guide — that applied the nav highlight mid-navigation and swallowed the first nav click. Now starts the guide in place and lets the engine's nav-hop logic highlight+walk (identical to working "ask Q" flow). Shaun confirmed FIXED. (`startGuideAfterNavigation` helper staged in startGuide.ts but unused — fallback for future.)
- **Q onboarding (`1a55901`):** new "where do I start / I'm new" branch in `orchestrator.ts` system prompt — Q asks discovery Qs (trade, quoting method, orders/invoices, volume) then maps to tools. **Shaun feedback: too long-winded, asks too many at once — Shaun to propose a solution. NOT YET REVISED.**

### Billing / account (`7b20691`)
- Account tab **"Notifications" → "Q Assistant"** (tab now only holds the Q on/off toggle; alerts live in Message Center settings).
- **Trial rolls into FREE (active), not read-only canceled:** mig `20260612190000` — `company_effective_plan_code` + `company_effective_plan_active` resolve expired-trial-no-stripe to `free` + active (was `starter` + read-only). `expire-trials` cron now sets `plan_code='free', subscription_status='active'` (was `canceled`). `EntitlementBanner` no longer shows the red "trial expired" lock for this case.
- New `TrialRolledToFreeBanner` (`app/components/billing/`): dismissible amber "you're on Free now" banner, reappears once per fresh login (sessionStorage). Layout shows it when `effectivePlanCode==='free' && trialEndsAt < now`.

### Send "test it first" tip (`67edb0e`)
- One-time tip, fires on FIRST send of ANY quote/order/invoice, then never again. mig `20260612200000` `users.send_test_tip_seen_at`.
- `app/components/send/`: `SendTestTipModal` (informational, X/Esc/"Got it" all proceed; copy adapts canEmail vs URL-only), `useSendTestTip` hook + module-level session flag (showing on one button suppresses the others in-session), `sendTestTip-actions.ts` (stamp flag).
- Wired into all 3 send buttons' open handlers; `canEmail` (`features.email_send`) + `sendTestTipSeen` threaded from quote-summary / order-preview / invoice-editor server pages.

## Migrations applied this session (shared dev+prod DB)
- `20260612180000_users_tutorials_seen_at`
- `20260612190000_trial_rolls_into_free` (2 SQL functions)
- `20260612200000_users_send_test_tip`
Types regenerated after each.

## Open / next
- **GERALD FULL AUDIT** is the next action — brief written: `docs/audits/GERALD-BRIEF-2026-06-12.md`. Scope = everything since Gerald's last proper audit (2026-06-01): Pricing Tier v2, Message Center, Invoices, Activity cards, Follow-up system + pg_cron, PDF pipeline, AI Assistant re-arch (never security-audited), Tutorials, trial→Free, send-tip.
- Q onboarding discovery questions need shortening (Shaun owns the solution).
- Smoke items added to `docs/smoke-tests/CHECKLIST.md` for tutorials + billing/trial + send-tip (verify on dev).

## Cost discipline (this session)
- Single long session — **exceeded the 30-turn / 250k-context cap** (context hit ~267k by wrap). Reason: continuous interactive build+review loop with Shaun across 5 push cycles; no natural break to restart. Model: Opus 4.8 throughout (judgment-heavy: billing-function migration, nav-blocking root-cause, multi-file send wiring). Sonnet not used. **Next session: start fresh** (this note is the handoff).
