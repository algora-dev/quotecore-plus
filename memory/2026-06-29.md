# memory/2026-06-29.md

## Features Shipped
- **Email confirmation flow (Task 2)**: signup now requires email confirmation. `email_confirm: false` in `createUser` → Supabase sends branded confirmation email → user clicks link → `/auth/callback` exchanges code for session → sends welcome email via Resend → redirects to workspace. Login page shows "Check your email" banner when `signup=pending`. Google signups get welcome email after onboarding completes (no confirmation needed — Google already verified).
  - New file: `app/lib/email/templates/welcome.ts` — branded welcome email with tutorials link + Q assistant prompt
  - Modified: `app/signup/actions.ts`, `app/auth/callback/route.ts`, `app/(auth)/onboarding/actions.ts`, `app/login/page.tsx`
- **Searchable flashing select (Task 4)**: new `SearchableFlashingSelect` component replaces plain `<select>` dropdowns in order editor (inline + AddItemModal). Text input filters grouped results in real-time.
  - New file: `app/components/SearchableFlashingSelect.tsx`
  - Modified: `app/(auth)/[workspaceSlug]/material-orders/create/order-create-form.tsx`
- **Hide pricing in order-from-quote (Task 3)**: `quote-lbl-loader.ts` now defaults `hideLinePrices: true, hideTotals: true` for order-from-quote. "Show all pricing" master toggle added to `OrderLineByLineEditor.tsx` (unticked by default).
- **Delete library modal text (Task 5)**: updated warning text in `component-list.tsx` to match Shaun's spec.
- **Docs sync (Task 1)**: updated `billing.mdx`, `tier-limits.mdx`, `trial.mdx`, `upgrading-and-cancelling.mdx` to match actual DB state. Key corrections: added Free plan to tables, corrected Starter storage (500MB not 200MB), corrected component/flashing/material order caps per tier, trial collapses to Free (not Starter).

## Decisions
- Supabase auth config already had `mailer_autoconfirm: false` and SMTP via Resend configured — no dashboard changes needed. Just set `email_confirm: false` in `createUser`.
- Welcome email sent in auth callback (not at signup) so we know the email is valid before sending. Detected via `email_confirmed_at` within 5-minute window.
- Google signups get welcome email in `completeGoogleOnboarding` (not callback) since they don't go through email confirmation.
- Premium plan kept as "coming soon" per Shaun's confirmation.
- Task 5 delete function was already built and working — just updated the modal text.

## Changes Approved By Shaun
- Keep Premium plan in docs (coming soon)
- Email confirmation flow approach (confirm email → activate → welcome email)
- All 5 tasks green-lit

## Bugs / Risks
- The `useSearchParams` hook in login page required wrapping in `Suspense` boundary for Next.js 16 build — done.
- `company.slug` in onboarding actions is typed `string | null` — added `|| 'workspace'` fallback.
- Entitlements.ts line 285 still falls back to `'starter'` instead of `'free'` — left as-is since the DB function `company_effective_plan_code` handles the real collapse logic and this is just a safety net.

## Follow-Ups
- ~~Stripe LIVE prices~~ ✅ VERIFIED 2026-06-29: all 4 plans have live price IDs, drift check passed with live key ($19/$39/$59 match). Growth has live price too but is `active=false`.
- ~~dev → main merge~~ ✅ DONE 2026-06-29: merged to main (`2aa6883`), pushed, Vercel auto-deploying.
- Smoke test items added to `docs/smoke-tests/CHECKLIST.md` — Shaun testing now.
- **Growth plan** is `active=false` — not shown on billing page. Plan ladder: Trial → Free → Starter ($19) → Professional ($39) → Pro Plus ($59) → Premium (coming soon).
- **Entitlements.ts** line ~285 falls back to `'starter'` instead of `'free'` — low priority since DB function handles the real collapse logic.

## Commits
- `d2464ef` — all 5 tasks + docs sync
- `7037604` — smoke test checklist items
- `96b426a` — fix docs: remove Growth, fix checkout description
- `2aa6883` — merge to main
- `d19dd02` — onboarding data-loss guard, email confirmation UX, Copilot→Q rebrand, Flashings→Drawings & Images in docs, rate limit raised to 500/hour
- `7e8c26e` — smoke test checklist update

## Afternoon session (16:09–16:40)

### Issues reported by Shaun during testing
1. **"Flashings" in docs/help billing table** — docs content still used "Flashings" instead of "Drawings & Images" in generic-trade context.
2. **Searchable dropdown** — Shaun couldn't test (data loss occurred); component already has search bar + scrollable list.
3. **Old Copilot info on Google signup flow** — `GoogleOnboardingForm.tsx` Step 3 still said "Meet Copilot" instead of Q + Tutorials.
4. **Google sign-in bypassing email confirmation** — by design (Google verifies email). Not a bug.
5. **Manual signup — can't log in + no email** — `rate_limit_email_sent` was 2/hour (way too low). Raised to 500/hour.
6. **"Server Components render error"** — unconfirmed users got cryptic error instead of friendly message.

### CRITICAL: Data loss incident
- Shaun manually deleted `public.users` row for `secarter23@gmail.com` but NOT the `auth.users` row.
- When he logged in via Google on the login page (which had `?signup=pending`), Google matched the existing auth user, redirected to `/onboarding`, which showed `GoogleOnboardingForm` (no profile), which created a **new company** and overwrote the old profile.
- Old company (with all flashings, quotes, components) was orphaned and is **unrecoverable** (no soft-delete on companies table).
- **Fix**: added data-loss guard in `completeGoogleOnboarding` — if user already has a `company_id` and the company exists, redirect to it instead of creating a new one.

### Fixes shipped (commit `d19dd02`)
- `completeGoogleOnboarding`: data-loss guard prevents company overwrite
- `login/actions.ts`: catch "Email not confirmed" → return `EMAIL_NOT_CONFIRMED` sentinel instead of throwing
- `login/page.tsx`: amber banner with "Resend confirmation email" option
- `login/actions.ts`: new `resendConfirmationAction` server action
- `GoogleOnboardingForm.tsx`: Step 3 rebranded from Copilot to Q + Tutorials (matches `OnboardingForm` welcome step); buttons changed to `rounded-full` + hover glow (design system)
- Docs: "Flashings" → "Drawings & Images" in `billing.mdx`, `tier-limits.mdx`, `trial.mdx`, `team.mdx`, `flashings.mdx`, `creating-orders.mdx`, `changelog.mdx` (generic-trade context only; roofing-specific changelog entries kept)
- Supabase auth: `rate_limit_email_sent` raised from 2 to 500/hour via Management API
- Cleaned up stale unconfirmed test accounts: `dev.algora@gmail.com`, `info@t3play.com` (auth users + orphaned companies deleted)

### Supabase auth config (verified)
- `mailer_autoconfirm: false` — email confirmation required for manual signups
- `mailer_allow_unverified_email_sign_ins: false` — unconfirmed users cannot sign in
- Google OAuth: `external_google_enabled: true` — Google users auto-confirmed (standard OAuth behaviour)
- SMTP via Resend: `smtp_host: smtp.resend.com`, `smtp_sender_name: QuoteCore+`
- `rate_limit_email_sent: 500` (raised from 2)

## Late afternoon session (17:08–20:14)

### Shaun's second round of testing feedback (17:08)
- Asked for comprehensive auth flow protection: returning Google users should be logged in, not create new accounts. Already-signed-up emails should be blocked at signup.

### Fixes shipped (commit `8af040a`)
- Three layers of orphan recovery: `/auth/callback`, `/onboarding` page, and `completeGoogleOnboarding` all check for orphaned quotes (via `quotes.created_by_user_id`) before creating a new company. If found, profile is restored and user redirected to existing workspace.
- `signupWithCompany`: friendlier error for already-registered emails.
- All `.single()` calls on potentially-missing rows changed to `.maybeSingle()`.

### Shaun's third round of testing feedback (19:10) — STILL BROKEN
1. **Google sign-in still bypasses email confirmation** — Shaun signed up with Google (info@t3play.com), was not asked to confirm email, went straight to onboarding. He wants Google users to also receive a confirmation email with a link/button before they can log in and complete signup.
2. **Welcome email has no confirmation button/link** — the welcome email arrived but has no link to confirm the email. The confirmation email template exists in Supabase config (has "Confirm email" button), but Shaun may not have received it due to rate limiting per email address, or the flow isn't triggering it correctly.
3. **"Server Components render error" STILL appears** — when trying to log in with an unconfirmed manual signup, the error still shows. The fix I shipped (throwing `EMAIL_NOT_CONFIRMED`) doesn't work because Next.js production builds sanitize thrown server action errors.
4. **Supabase-branded URL in Google OAuth flow** — during Google sign-in, the browser shows a `*.supabase.co` URL. Shaun wants a QuoteCore+ branded URL.
5. **Drawings & Images upload UX** — plain `<input type="file">` inconsistent with rest of app.
6. **After getting the error, clicked Google login → went straight to account** — confirms Google bypasses email confirmation entirely.

### Fixes shipped (commit `445ad03`)
- **Login error fix**: `loginAction` now RETURNS a structured `LoginResult` object instead of throwing for known error states. Client reads the return value, not a caught error. This should fix the "Server Components render error".
- **Drawings & Images upload**: replaced plain file input with drag-and-drop zone matching `FileUploader` pattern.
- **Signup pre-check**: `signupWithCompany` checks `public.users` table for existing email before calling `createUser`.
- **Gerald brief written**: `docs/audits/GERALD-BRIEF-2026-06-29-AUTH-FLOW.md`

### STILL UNRESOLVED (for next session / Gerald)
1. **Google email confirmation** — Shaun wants Google users to also confirm email before completing signup. Currently Google users are auto-confirmed by Supabase (standard OAuth). Options in Gerald brief.
2. **Supabase-branded URL** — needs custom domain config on Supabase (CNAME like `auth.quote-core.com`). Not a code fix.
3. **Login error fix not yet verified by Shaun** — the return-vs-throw fix (`445ad03`) was pushed but Shaun hasn't tested it yet.
4. **Confirmation email not received** — Shaun didn't receive a confirmation email for info@t3play.com after manual signup. Possible causes: per-email rate limiting (Resend), email going to spam, or the signup flow not triggering the confirmation email correctly. Need to verify.
5. **Welcome email has no confirmation link** — the welcome email is sent AFTER confirmation (in `/auth/callback`), so it doesn't need a confirm button. But the CONFIRMATION email (sent by Supabase at signup time) should have the button. Need to verify which email Shaun actually received.

### Commits (late afternoon)
- `8af040a` — three-layer orphan recovery, signup friendly error, .maybeSingle() fixes
- `445ad03` — login return-vs-throw fix, drag-drop upload UX, signup pre-check, Gerald brief

### Gerald brief
- File: `docs/audits/GERALD-BRIEF-2026-06-29-AUTH-FLOW.md`
- Covers: login error handling, Google email confirmation, Supabase URL branding, orphan recovery security, signup race safety, middleware auth states
- Status: READY FOR GERALD — Shaun needs to kick off the audit
