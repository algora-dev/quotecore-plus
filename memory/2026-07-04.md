# memory/2026-07-04.md

## Features Shipped
- **9-Phase Takeoff Area Fixes** (commits `c4ee521`, `7583dc0` on development):
  - Phase 1: Layout fix (h-screen + overflow-hidden + min-h-0 for proper flex chain)
  - Phase 2: DB-first area naming (createNewTakeoffArea accepts optional label with dedup)
  - Phase 3: Area sub-measurement rows (RPC migration `20260704130000`, creates quote_roof_area_entries per polygon, area-scoped delete)
  - Phase 4: Save/restore all areas (auto-save on switch, flush cached areas on full save)
  - Phase 5: Area delete (trash icon, ConfirmModal, deleteTakeoffArea server action)
  - Phase 6: "+ New Area" choice modal (add to existing dropdown / create new with name after drawing)
  - Phase 7: "Save & upload another plan" (area dropdown, no upfront name for new)
  - Phase 8: Multi-plan re-entry (pageIds cached per area, page switch on area click)
  - Phase 9: Build passes, smoke test checklist updated

## Decisions
- RPC no longer creates quote_roof_areas rows — areas created upfront by createNewTakeoffArea
- Each drawn polygon creates a quote_roof_area_entries row (sub-measurement system)
- deleteTakeoffArea detaches components/pages (sets NULL) rather than deleting them
- Auto-save on area switch is best-effort (failures logged, don't block switch)

## Changes Approved By Shaun
- 9-phase plan confirmed before building
- Clean up orphaned/duplicate areas on "Updated Digi Test" quote (pre-test cleanup)

## Bugs / Risks
- RPC migration replaces save_takeoff_atomic entirely — old behaviour lost (rollback via re-applying 20260526160000)
- Auto-save on switch sends measurements without canvas snapshot — canvas images only saved on full "Save & Continue"
- Phase 6 choice modal routing depends on pendingNewAreaIsExisting flag — needs careful testing with existing flows
- createTakeoffPageForArea still exists in actions.ts but is no longer called for new-area uploads (Phase 7 uses createTakeoffPage only)

## Fixes Tried
- Cleaned up 5 orphaned/duplicate areas on "Updated Digi Test" quote (3 kept: Main House, Garage 2, Garage 3)

## Follow-Ups
- Smoke test the 9-phase build on Dev
- After smoke test passes: merge development → main
- Consider retiring createTakeoffPageForArea entirely if Phase 7 flow proves stable
- Consider adding NOT NULL constraint on quote_takeoff_measurements.quote_roof_area_id in a future migration

## Promote To MEMORY.md?
- Takeoff 9-phase area fixes shipped (update existing MEMORY.md entry)
- RPC no longer creates quote_roof_areas rows (durable architectural change)
- deleteTakeoffArea action exists (durable)
